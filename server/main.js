import { Meteor } from 'meteor/meteor';
import "./publicaciones"

import "./rutas"
import "./metodos"
import "./startup"
import "./tareas"


if (Meteor.isServer) {
Meteor.startup(async () => {

    
});
}